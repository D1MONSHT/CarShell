<Window x:Class="CarShell.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="CarShell"
        WindowState="Maximized"
        WindowStyle="None"
        ResizeMode="NoResize"
        Background="#05080D">

    <Window.Resources>
        <Style x:Key="SideButton" TargetType="Button"> 
            <Setter Property="Width" Value="76"/>
            <Setter Property="Height" Value="76"/>
            <Setter Property="Margin" Value="12,10"/>
            <Setter Property="FontSize" Value="36"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="#111923"/>
            <Setter Property="BorderBrush" Value="#27384A"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border CornerRadius="22"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PlayerButton" TargetType="Button">
            <Setter Property="Width" Value="80"/>
            <Setter Property="Height" Value="80"/>
            <Setter Property="Margin" Value="12,0"/>
            <Setter Property="FontSize" Value="30"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="#111923"/>
            <Setter Property="BorderBrush" Value="#27384A"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border CornerRadius="18"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0"
                Background="#080D14"
                BorderBrush="#1C2A38"
                BorderThickness="0,0,2,0">

            
        </Border>

        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="95"/>
            </Grid.RowDefinitions>

            <ContentControl x:Name="MainContent"
                            Grid.Row="0"/>

            <Border Grid.Row="1"
                    Background="#080D14"
                    BorderBrush="#1C2A38"
                    BorderThickness="0,0,2,0">

                <Grid Margin="24,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="100"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>



                    <StackPanel
                                Orientation="Horizontal"
                                VerticalAlignment="Top" Grid.RowSpan="1">

                        <Button Content="⌂" Click="Home_Click" Style="{StaticResource SideButton}"/>
<Button Content="1" Click="Navigation_Click" Style="{StaticResource SideButton}"/>
               
                     
                        <Button Content="!" Click="Board_Click" Style="{StaticResource SideButton}"/>
                        <Button Content="!!" Click="Error_Click" Style="{StaticResource SideButton}"/>
                        <Button Content="⚙" Click="Settings_Click" Style="{StaticResource SideButton}"/>

                    </StackPanel>


                </Grid>
            </Border>
        </Grid>
    </Grid>
</Window>